## How to set up local development environment with Laravel Valet

### Installation

1. Run `valet proxy app.tcrss http://localhost:3000 --secure` to create proxy site.
2. Try to access https://app.tcrss.test/ to make sure it works.
